[workspace]
resolver = "2"

members = [
    "ckb_deterministic",
    "example_contracts/deterministic_cdp_project",
    "example_libs/deterministic_cdp_shared",
]


[profile.release]
overflow-checks = true
strip = true
codegen-units = 1
panic = "abort"

[profile.dev]
panic = "abort"
